[project]
name = "bank-alfa-voice-mortgage"
version = "0.1.0"
description = "Deterministic two-view voice mortgage demo for fictional Bank Alfa"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py312"
line-length = 120